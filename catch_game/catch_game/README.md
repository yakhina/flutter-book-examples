# 🎮 Catch Game — пример игры из книги по Flutter

Проект создан для главы **6. “Ну, лови!”** книги по Flutter-разработке.
Это простая аркадная игра, в которой пользователь управляет “ракеткой” и ловит падающий мяч.
На этом примере изучается работа с **анимацией, жестами и координатами** во Flutter без использования сторонних игровых движков.

---

## 📘 Что демонстрирует проект

* использование **GestureDetector** для перемещения объектов;
* ручная анимация через **Ticker**;
* отскоки и столкновения (hitbox);
* подсчёт очков и игровая логика;
* базовая работа с координатами и ограничениями по экрану.

---

## 🧩 Структура проекта

```
lib/
 ├── main.dart              # Точка входа — запускает игру
 ├── game_page.dart         # Основной экран с логикой и отрисовкой
 └── models/
      ├── game_object.dart  # Базовый класс игрового объекта (мяч)
      └── paddle_object.dart# Класс ракетки игрока
```

---

## 🚀 Как запустить

1. Установите зависимости:

   ```bash
   flutter pub get
   ```

2. Запустите приложение:

   ```bash
   flutter run
   ```

---

## 🎯 Обучающая цель

Пример показывает, как использовать **чистый Flutter SDK** для создания небольшой игры без библиотек вроде Flame.
На практике изучаются:

* Ticker и ручная анимация;
* отрисовка объектов в Stack;
* реакции на касания и перетаскивания;
* расчёт столкновений и физики движения.

---

## 🧱 Технологии

* **Flutter SDK**
* **Dart**
* **Material Design**
* **GestureDetector**
* **Ticker**

---

## 📝 Лицензия

Проект создан для учебных целей в составе книги по Flutter.
Код можно использовать и изменять в образовательных и некоммерческих проектах.
