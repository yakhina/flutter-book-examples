# 🌤️ Weather App — Простое приложение погоды

Это учебное Flutter‑приложение, созданное в рамках главы **5 — Работа с API, HTTP‑запросами и JSON**.  
Оно показывает текущую погоду в выбранном городе, используя:

- Open‑Meteo API — для получения температуры и скорости ветра  
- Nominatim (OpenStreetMap) — для геокодинга названия города  
- пакет `http` для выполнения сетевых запросов  
- структуру из двух сервисов: `WeatherService` и `GeocodingService`

---

## 📁 Структура проекта

```
weather_app/
├── lib/
│   ├── main.dart                 # главный экран приложения
│   ├── services/
│   │   ├── geocoding_service.dart   # сервис для получения координат по названию города
│   │   └── weather_service.dart     # сервис для получения погоды по координатам
│   └── widgets/ (опционально)       # дополнительные виджеты, если расширять проект
│
├── pubspec.yaml                     # описание зависимостей и ресурсов
└── README.txt                       # этот файл
```

---

## 🚀 Возможности приложения

- Ввод города вручную  
- Получение координат через Nominatim API  
- Запрос текущей погоды через Open‑Meteo  
- Отображение:
  - температуры  
  - скорости ветра  
- Обработка ошибок (например, если город не найден)

---

## 🔧 Используемые технологии

- **Flutter**  
- **Dart**
- **HTTP API**
- **JSON**
- **Open‑Meteo API**
- **Nominatim API (OpenStreetMap)**

---

## 📦 Подключение зависимостей

Для работы с HTTP-запросами используется пакет:

```
flutter pub add http
```

Также можно вручную добавить в `pubspec.yaml`:

```yaml
dependencies:
  http: ^1.4.0
```

---

## ▶️ Запуск

```bash
flutter create weather_app
cd weather_app
flutter run
```

---

## 🧪 Что изучается в проекте

- работа с внешними API  
- построение GET-запросов  
- разбор JSON-ответов  
- структура проекта с сервисами  
- использование pubspec.yaml  
- обработка ошибок в сетевых операциях  

---

## 📚 Соответствие главе книги

Проект соответствует разделам:

- **5.1 — Что такое API**
- **5.2 — Зачем нужен API**
- **5.3 — Пакеты и pubspec.yaml**
- **5.4 — Open‑Meteo API**
- **5.5 — Геокодинг**
- **5.6 — Интерфейс приложения**

---

## ✨ Идеи для расширения

- Добавить прогноз на 3/7 дней  
- Сохранение последних запросов  
- Автоопределение города по геолокации  
- Погодные иконки  
- Темная/светлая тема  

---

## Лицензия

Проект является частью учебных материалов книги.  
Разрешено использовать и изменять код в образовательных и некоммерческих целях.
