# City Quest — пример приложения из книги по Flutter

Этот проект создан как практический пример для книги о Flutter-разработке.  
Он показывает, как построить приложение **«Квест по городу»** с использованием локального хранилища, архитектуры с разделением слоёв, сервисов и карт **Yandex MapKit**.

---

## 📘 Описание проекта

Проект демонстрирует:

* работу со структурированной архитектурой (Domain / Data / Presentation);
* использование **Hive** для локального хранения данных;
* создание моделей и адаптеров для Hive;
* подключение и настройку **Yandex MapKit**;
* работу с координатами и расчётом расстояний;
* управление состоянием через **BLoC**;
* организацию сервисов (например, геолокации);
* построение отдельных фич: список городов, точки квеста, экран карты.

Код проекта сопровождает главы книги, где каждая часть добавляет новые возможности.

---

## 🧩 Структура проекта

```
lib/
 ├── core/                      # Общие компоненты (DI, утилиты)
 ├── domain/                    # Бизнес-логика
 │     ├── entities/            # Доменные модели
 │     ├── usecases/            # Сценарии использования
 │     └── repositories/        # Интерфейсы репозиториев
 ├── data/
 │     ├── models/              # Модели для Hive
 │     ├── datasources/         # Источники данных
 │     └── repositories/        # Реализации репозиториев
 ├── features/
 │     ├── quest/               # Фича квестов
 │     └── map/                 # Фича карты
 ├── services/                  # Системные сервисы (геолокация и др.)
 ├── app/                       # Конфигурация приложения
 └── main.dart                  # Точка входа
```

---

## 🚀 Возможности

* Загрузка и хранение точек квеста и городов
* Расчёт расстояния до точки
* Определение достижения радиуса
* Просмотр городов и статистики по ним
* Экран с картой на **Yandex MapKit**
* Показ ближайших точек
* Управление состоянием через BLoC
* Проверка геолокации и работа с GPS

---

## ⚙️ Как запустить проект

1. Установите зависимости:

```bash
flutter pub get
```

2. Сгенерируйте адаптеры Hive:

```bash
dart run build_runner build --delete-conflicting-outputs
```

3. Проверьте, что в `assets/mocks/` есть файлы:

```
cities.json
quests.json
```

4. Запустите приложение на Android или iOS:

```bash
flutter run
```

> **Важно:** Yandex MapKit не работает на Web. Приложение запускается только на iOS и Android.

---

## 📖 Обучающая цель

Проект показывает практическое применение:
* разделения слоёв,
* работы с геоданными,
* подключения внешних SDK,
* использования BLoC,
* организации архитектуры, удобной для масштабирования.

---

## 🧱 Используемые технологии

* **Flutter**
* **Dart**
* **Hive**
* **Yandex MapKit**
* **BLoC**
* **GetIt (Service Locator)**

---

## 📝 Лицензия

Проект распространяется для учебных целей в составе книги.  
Код можно использовать в образовательных и некоммерческих проектах.
