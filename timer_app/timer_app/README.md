# Таймер и Секундомер — приложение из книги по Flutter

Этот проект создан как практический пример для главы 4 книги по Flutter-разработке.  
В нём шаг за шагом реализуются две ключевые функции — **Секундомер** и **Таймер обратного отсчёта**, а также подробно разбираются Stateful Widgets, жизненный цикл состояния, работа с Ticker и таймерами.

---

## 📘 Описание проекта

Приложение демонстрирует:

- использование **StatefulWidget** и ключевых методов жизненного цикла (`initState`, `build`, `setState`, `dispose`);
- работу с **Ticker** для высокоточного обновления времени (секундомер);
- использование **Timer.periodic** для создания таймера обратного отсчёта;
- создание вкладок через **TabBar**, **TabController** и **TabBarView**;
- применение **SingleTickerProviderStateMixin** для управления анимациями;
- построение интерфейса с использованием **Material Design**;
- раздельную структуру проекта: главный экран + два отдельных файла для таймера и секундомера.

Этот пример показывает, как создавать динамические интерфейсы, которые обновляются в реальном времени и используют анимационный цикл Flutter.

---

## 🧩 Структура проекта

lib/
├── main.dart # Главный экран и логика TabBar
├── stopwatch_screen.dart # Экран секундомера (Ticker)
└── timer_screen.dart # Экран таймера обратного отсчёта (Timer.periodic)


---

## ⏱️ Возможности

### Секундомер
- Старт
- Пауза
- Сброс
- Отображение времени в формате **мм:сс:мс**
- Обновление времени через Ticker (60 FPS)

### Таймер
- Обратный отсчёт от 60 секунд
- Пауза
- Сброс
- Отображение оставшегося времени в секундах
- Реализация через Timer.periodic

### Общее
- Две вкладки: **Таймер** и **Секундомер**
- Удобная навигация через TabBar снизу
- Свайпы между вкладками через TabBarView

---

## ⚙️ Как запустить проект

1. Установите зависимости:

```bash
flutter pub get

2. Запустить приложение:

```bash
flutter run

3. Запустить в браузере:

```bash
flutter run -d chrome

---

## 📖 Обучающая цель проекта

Проект помогает изучить:

- работу со StatefulWidget;
- управление состоянием;
- построение интерфейса с вкладками;
- использование Ticker и Timer;
- обновление UI в реальном времени.

---

## Используемые технологии

- Flutter
- Dart
- Stateful Widgets
- Ticker
- Timer.periodic
- TabBar / TabController / TabBarView

---

## Лицензия

Проект является частью учебных материалов книги.  
Разрешено использовать и изменять код в образовательных и некоммерческих целях.
