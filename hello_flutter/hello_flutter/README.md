# Привет, Flutter! — первое приложение из книги по Flutter

Этот проект создан как самый первый практический пример в книге по Flutter-разработке.  
В нём демонстрируется создание минимального приложения, знакомство с деревом виджетов, Material Design и основами работы со Stateful-виджетами.

---

## 📘 Описание проекта

Приложение показывает:

- запуск приложения через `main()` и `runApp()`;
- работу с виджетами **StatelessWidget** и **StatefulWidget**;
- использование базовых виджетов **MaterialApp**, **Scaffold**, **AppBar**;
- построение интерфейса через **Column**, **Text**, **SizedBox**;
- работу с прокруткой (`SingleChildScrollView`);
- добавление кнопки (**ElevatedButton**) и обработчика нажатия;
- отображение диалога (**AlertDialog**) через `showDialog()`.

Этот пример служит фундаментом для последующих приложений книги.

---

## 🧩 Структура проекта

    lib/
      └── main.dart        # Основной файл приложения

*В данном примере весь код сосредоточен в одном файле, как в книге. При желании экран можно вынести в отдельный файл.*

---

## 🚀 Возможности

- Приветственный текст
- Прокручиваемое содержимое экрана
- Кнопка с обработчиком события
- Всплывающее диалоговое окно
- Простая структура интерфейса на Material Design
- Демонстрация `StatefulWidget`

---

## ⚙️ Как запустить проект

1. Установите зависимости:

       flutter pub get

2. Запустите приложение:

       flutter run

   Чтобы открыть в браузере:

       flutter run -d chrome

---

## 📖 Обучающая цель проекта

Проект помогает понять:

- как устроена структура Flutter-приложения;
- как работает дерево виджетов;
- как применять Stateless и Stateful виджеты;
- как создавать интерфейс с помощью `Scaffold`, `Column` и `Text`;
- как добавлять кнопки и диалоги;
- как отображать содержимое с прокруткой.


---

## 🧱 Используемые технологии

- **Flutter**
- **Dart**
- **Material Design**
- **Stateless / Stateful Widgets**

---

## 📝 Лицензия

Проект распространяется как часть учебных материалов книги.  
Разрешено использовать и модифицировать код в образовательных и некоммерческих целях.
